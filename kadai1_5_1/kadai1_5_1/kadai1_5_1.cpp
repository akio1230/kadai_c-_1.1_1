#include <iostream>
#include "template.h"

int main()
{
    // int 型の計算
    add(1, 2);
    sub(1, 2);
    mul(1, 2);
    div(2, 2);

    // float型の計算
    add(1.5f, 1.5f);
    sub(2.0f, 1.0f);
    mul(3.0f, 3.0f);
    div(5.0f, 2.5f);





    return 0;
}

